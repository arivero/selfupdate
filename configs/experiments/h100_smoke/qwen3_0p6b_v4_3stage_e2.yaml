# v4 stage-count coverage: THREE processes (cards 0-2), 2 epochs, relay +
# battery. With the 4-stage leg this proves the stage count is truly free
# (cuts [9, 18] give 9/9/10 blocks - uneven on purpose).
run_name: h100_smoke_qwen3_0p6b_v4_3stage_e2
mask:
  compaction: flow_mask
cache:
  store_full_teacher_inputs: true
train:
  pipeline_version: 4
  pipeline_revision: "4.0"
  batching: bucketed
  epochs: 2
  v4_stage_splits: [9, 18]
  v4_stage_devices: [0, 1, 2]
