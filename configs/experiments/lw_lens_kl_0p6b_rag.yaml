# lens-KL candidate: every block matches the TEACHER's layer-L lens
# distribution — behavioral target, strictly local, softer than gold-CE
# (which failed at a single block). PKD through the readout.
run_name: lw_lens_kl_0p6b_rag
train:
  method: layerwise
  schedule: summed
  epochs: 40
  lr: 1.0e-5
  lens_kl_weight: 0.5
  last_block_ce_weight: 0.5
  lora:
    enabled: false
