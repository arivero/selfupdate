# eval-only: maieutic elicitation probe at 4b (model.name must match the
# LoRA arm — evaluate.py rebuilds base+adapters from cfg.model.name)
run_name: eval_maieu_4b
model:
  name: Qwen/Qwen3-4B
data:
  examples_path: data/poem/examples_maieu.jsonl
  maieutic: true
