# Campaign40 primary arm: gemma-4-31B-it (dense, 60L x 5376) PPP4, 40 epochs.
# Transformation applied from configs/experiments/h100_smoke/
# gemma4_31b_v4_ppp4_evalin.yaml the same way gemma4_26b_v4_ppp4_e40.yaml
# relates to gemma4_26b_v4_ppp4_evalin.yaml: epochs 3->40, eval.every_epochs
# and standard_damage_every_epochs 1->5. Adds lr: 3.0e-6 (the train_sweep
# winning LR). micro_batch: 8 preserved from the proven 31B evalin config
# (not 16 -- a per-model tuned value, not touched). Proven stage split:
# [15, 30, 45] over 4 devices.
run_name: campaign40_g31b
mask:
  compaction: flow_mask
cache:
  store_full_teacher_inputs: false
train:
  pipeline_version: 4
  pipeline_revision: "4.6"
  batching: bucketed
  micro_batch: 8
  epochs: 40
  seed: 17
  v4_teacher_source: store
  v4_loop_order: layer_major
  v4_relay_every_cohorts: 3
  v4_stage_scoped: true
  v4_weight_residency: auto
  v4_stage_splits: [15, 30, 45]
  v4_stage_devices: [0, 1, 2, 3]
  v4_optimizer: immediate_sgd
  lr: 3.0e-6
eval:
  every_epochs: 5
  recall_corpora: [machado, quijote_ch1, quijote_ch4]
  standard_damage_every_epochs: 5
