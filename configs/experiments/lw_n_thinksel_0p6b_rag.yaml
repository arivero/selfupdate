# P3 arm: thinking_selective x final recipe k8 + v2 anchors. Does keeping
# free deduction in the student view fix the think-channel bypass?
run_name: lw_n_thinksel_0p6b_rag
data:
  examples_path: data/poem/examples_think_sel.jsonl
train:
  method: layerwise
  schedule: summed
  epochs: 40
  lr: 1.0e-5
  hidden_loss: vocab_mse
  tail_ce_blocks: 8
  tail_ce_weight: 0.5
  anchor_kl_weight: 0.5
  anchor_path: data/anchors_es_v2.txt
  frozen_teacher_copy: true
  lora:
    enabled: false
