# eval-only: maieutic elicitation probe at 8b (model.name must match the
# LoRA arm — evaluate.py rebuilds base+adapters from cfg.model.name)
run_name: eval_maieu_8b
model:
  name: Qwen/Qwen3-8B
data:
  examples_path: data/poem/examples_maieu.jsonl
  maieutic: true
