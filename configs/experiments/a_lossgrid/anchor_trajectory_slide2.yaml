run_name: a_lossgrid_1p7b_combined_slide2_anchor_trajectory
train:
  hidden_loss: nmse
  conn_window: 2
  conn_stride: 1
  readout_window_blocks: 2
  readout_source: teacher_kl
  # Keep the existing output anchor and add equal, depth-uniform hidden
  # preservation against the frozen base on every optimizer boundary.
  anchor_kl_weight: 0.5
  anchor_hidden_weight: 0.5
eval:
  recall_corpora: [machado, quijote_ch1, quijote_ch4]
