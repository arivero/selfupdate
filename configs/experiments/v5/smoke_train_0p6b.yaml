# v5 pipeline smoke arm (3 optimizer steps); see smoke_cache_0p6b.yaml.
run_name: v5smoke_0p6b_huber2_window_remove
data:
  examples_path: data/combined/examples_v5_smoke.jsonl
mask:
  compaction: remove
train:
  hidden_loss: huber
  conn_window: 2
  readout_window_blocks: 2
  epochs: 1
  max_steps: 3
eval:
  standard_damage_every_epochs: 0
