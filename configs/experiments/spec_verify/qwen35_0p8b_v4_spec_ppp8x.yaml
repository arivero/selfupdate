# PPP8 cross-node (agpuh01 stages 0-3, agpuh02 stages 4-7): trainer-native
# vLLM check through the cross-node relay. Launch with
#   SELFUPDATE_V4_STAGE_HOSTS="agpuh01 agpuh01 agpuh01 agpuh01 agpuh02 agpuh02 agpuh02 agpuh02" \
#   scripts/launch_v4_stages.sh <base> <this>
# Expect teacher_argmax_acceptance = 0.9854211663066955 (bit-exact vs PPP1/2/4).
# Prereqs on agpuh02: venv (venv_setup.sh), shm HF stage of Qwen3.5-0.8B,
# and the index-only cache (build_teacher_cache --index-only --coordinated-node-cache).
run_name: spec_qwen35_0p8b_v4_ppp8x_e1
mask:
  compaction: flow_mask
model:
  device: cuda
train:
  pipeline_version: 4
  pipeline_revision: "4.0"
  trajectory_source: teacher_hidden
  update_granularity: online
  schedule: summed
  grad_accum: 1
  batching: bucketed
  v4_teacher_source: online
  v4_stage_splits: [3, 6, 9, 12, 15, 18, 21]
  v4_stage_devices: [0, 1, 2, 3, 0, 1, 2, 3]
