# PPP2: 2 stages (cut at 12), GPUs 0+1 — trainer-native vLLM check with the
# staged machinery (stage processes + relay) in the loop. teacher_argmax_
# acceptance must reproduce the PPP1 row (0.9854211663066955) bit-exactly.
run_name: spec_qwen35_0p8b_v4_ppp2_e1
mask:
  compaction: flow_mask
model:
  device: cuda
train:
  pipeline_version: 4
  pipeline_revision: "4.0"
  trajectory_source: teacher_hidden
  update_granularity: online
  schedule: summed
  grad_accum: 1
  batching: bucketed
  v4_teacher_source: online
  v4_stage_splits: [12]
  v4_stage_devices: [0, 1]
