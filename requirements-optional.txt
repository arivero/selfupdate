# Additional required packages for full project coverage.
# Kept separate from the core CUDA stack so the heavy torch install can be
# validated independently.

# HF benchmark/destruction/retention/tuned-lens data loaders.
datasets==5.0.0

# scripts/evaluate.py --load-4bit
bitsandbytes==0.49.2

# NVML bindings: torch.cuda.utilization() for the v4 training-phase
# utilization gate (train.v4_min_train_gpu_util). Without it the gate
# logs train_phase_gpu_util=null and cannot enforce the 50% floor.
nvidia-ml-py==13.610.43
