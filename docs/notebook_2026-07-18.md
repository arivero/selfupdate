# Experiment notebook — night of 2026-07-17/18 (v4 envelope expansion)

Live operator notebook. Every parameter adjustment gets a row with the
reason and the evidence line that forced it. Timezone = cluster local.

## Standing context

- Branch `lwteacher`, pipeline-v4 blockwise teacher-forced training.
- Nodes: agpuh01 + agpuh02 (both 4×H100 80GB, reserved tonight).
- Goals: (1) prove epoch speed supports 20–30 epochs at big-model scale;
  (2) demo ladder = answers with exact `token_ids` for every envelope model.
- 27B einf reference run: ~60 epochs completed, stopped deliberately at
  ~00:45 to free agpuh01 for framework testing. Learning verdict: hidden
  losses fell ~19%, teacher-forced CE flat at 0.0137, deployment CE flat
  2.228, quijote_ch1 recall 0.233→0.286(ep24)→0.262. Signal stored
  blockwise, weakly expressed → motivates lr/dose sweep (task #9).

## Demo ladder status (exact token_ids, TP4, vLLM 0.25, 2071 items)

| model | verdict | tok/s | notes |
|---|---|---|---|
| gemma-4-26B-A4B-it | PASS | — | task 0.954, containment 0.815 |
| gemma-4-31B-it | PASS | 964 | containment 0.752 (Gemma paraphrasing trait) |
| Qwen3.5-122B-A10B | PASS | 193 | containment 0.975 |
| DeepSeek-V4-Flash | attempt 4 pending | — | see 01:25 entry |
| Qwen3.5-397B-FP8 | n/a on 4 cards | — | download COMPLETE (379 GB) |

Gemma containment drop (0.75–0.82) flagged: run the RAG-gate control
before training on those answer sets.

## Parameter adjustments (chronological)

| when | knob | old → new | why |
|---|---|---|---|
| ~23:50 | launcher `HF_HUB_OFFLINE` | unset → 1 | Hub revalidation produced phantom "missing shard" on complete cache (`e6d4128`) |
| 00:5x | `v4_capture_chunk` (26B teacher forward) | whole-batch → 8 → 4 → 2 | teacher forward OOM on longest cohort (T~5070); Opus-debug commits `72706f7`, `6c9e431`, `037bd80` |
| 00:5x | mid-epoch utilization gate | no exemption → epoch-1 exempt | 26B stages 1/2 killed at 17.5%/18.8% util during epoch-1 warm-up (`07094c2`) |
| 01:0x | gemma-31B `micro_batch` | 32 → 16 | all 4 stages OOM in gemma4 sdpa attention at 78.4/79.20 GiB with ~59 GB resident weights (`3927eea`). NOTE: the 01:18 launch predated this commit (source_commit bc04900, micro_batch 32) and OOMed identically — relaunch with the fix still pending. |
| 01:3x | DeepSeek demo `--kv-cache-dtype` | auto → fp8 | vLLM 0.25 DeepSeek-V4 `fp8_ds_mla` layout asserts: "only supports fp8 kv-cache, got auto" (all 4 TP workers, attempt 3). Flag added to `benchmark_vllm_generation.py`. |
| 01:56 | DeepSeek demo `--moe-backend` | (marlin) → triton | fp4-expert Marlin repack kernels: "PTX compiled with an unsupported toolchain" under driver 565; Triton compiles at runtime. |
| 02:20 | gemma-31B `micro_batch` | 16 → 8 | stage 3 OOMed in EPOCH 2 at 78.5 GiB (epoch-2 transient peak differs from epoch 1); reaper then killed siblings at 01:58. |
| 02:20 | gemma-31B `v4_min_train_gpu_util` | 50 → 20 | auto residency = rebuild for dense 31B (cpu_stream would need ~1.9 TB/stage), so every epoch is recompute-bound (per-epoch teacher recompute) at ~25% util; the mid-epoch gate at 43.4% was killing honest physics. |

## Run log

- **01:17** dequant of DeepSeek-V4-Flash fp8→bf16 running on agpuh01 CPU
  (pid 1244763, ~2.6 cores) → `snapshots/deepseek-v4-flash-bf16`. Feeds
  the training lane (HF fp8 is `is_trainable=False`).
- **01:18** gemma-31B PPP4 relaunch on agpuh02 (launch id ...011823) OOMed
  again — it ran from `bc04900`, i.e. WITHOUT the micro_batch 16 fix.
  Zero v4_epoch rows. Will relaunch from HEAD after the DeepSeek demo
  frees agpuh02 (both want 4 cards).
- **01:21–01:25** DeepSeek demo attempt 3 on agpuh02: chatfmt fixes
  CONFIRMED (tokenizer + prompt build passed); died at engine init on the
  kv-cache dtype assert above. GPUs on agpuh02 now free.
- **01:25** Opus debugger's gemma-26B probe (stage 3 solo) running on
  agpuh01 GPU 3; agpuh01 GPUs 0–2 free.

- **01:35–01:36** DeepSeek demo attempt 4 (with `--kv-cache-dtype fp8`):
  passed the kv assert, then all TP workers died in the fp4-expert weight
  repack — "PTX compiled with an unsupported toolchain" (driver 565 cannot
  JIT the vllm025 build's quantized-weight kernels; bf16 models never touch
  them, which is why the other three demos passed). Open as task #14.
- **01:41** B8 committed (`9d7aa3c`): DeepSeek frozen-context adapter
  complete — typed-cache duck-type (zero-width chunk trick), teacher-forced
  indexer routing in a pre-extended mask, mHC stream plumbing, hash-MoE
  input_ids threading, fp32-strict in shard_load, V4 LoRA target fix
  (kv-side compressor/indexer submodules excluded). Cert gate pending the
  bf16 dequant snapshot (still writing, pid 1244763).
- **01:48** gemma-31B PPP4 relaunched on agpuh02 from HEAD with
  micro_batch 16 (launch id v4-20260718014810-3506841): all four GPUs at
  62–70 GB and computing within a minute — past the point where the B=32
  launch OOMed.
- **01:48** 0.6B v4 online 1-epoch smoke on agpuh01 cuda:0 (pid 1252089):
  regression check that tonight's shared-path refactor (run_block
  signature, flow-keep broadcast, dim-agnostic gathers, staged_to) is
  bit-benign for the standard lane.
- **01:48** Opus gemma-26B debugger told to stand down and deliver a
  compact diagnosis; its last probe (stage 3, agpuh01 GPU 3) left running
  to completion. Lane takeover is task #15.

- **01:53** 0.6B refactor smoke: **training PASS** — epoch 1 clean at
  commit 558c827 (which includes the B8 shared-path refactor): per-layer
  huber 4.6e-5..0.029, teacher-fwd 1.27 s, 36.9k token-events/s. Crashed only
  at checkpoint publish on a STALE checkpoint dir from an earlier session
  (tripwire working as designed). Stale dir moved aside, relaunched
  (smoke2, log runs/smoke_0p6b_refactor2.log) to green the checkpoint
  path too.
- **01:55 /home NFS incident (owner-reported):** shared /home mount is
  100% full cluster-wide. Our contributions (~13 MB of .triton/.cache/
  vllm compile caches + empty .singularity) removed on BOTH nodes;
  /home/arivero now 1.4 MB. `launch_v4_stages.sh` now pins
  TRITON_CACHE_DIR / TORCHINDUCTOR_CACHE_DIR / VLLM_CACHE_ROOT to
  /tmp/$USER (`e6dedc5`) so no training or demo job depends on writable
  /home. Singularity remains deprecated.
- **01:56** DeepSeek demo attempt 5 prepared: `--moe-backend triton`
  (Triton compiles kernels at runtime with its own toolchain, bypassing
  the Marlin PTX that driver 565 cannot JIT). Waits for a 4-GPU window
  (agpuh02 owned by gemma-31B training; agpuh01 pending 26B-probe +
  smoke2 completion). Triton cache now pinned to /tmp.
- **01:55** dequant progress note: pid 1244763 at 201 GB RSS, ~21 cores —
  it dequantizes fully in RAM and writes at the end; empty output dir is
  expected mid-flight.

- **02:07 gemma-26B verdict (Opus debugger final report, lane closed):**
  the first-run failure was a THREE-part cascade — (1) mid-epoch util gate
  lacked the epoch-1 exemption (fixed `07094c2`); (2) stage-3 "zero
  epochs" was NOT a bug, just the reaper cascade (its metrics show a
  partial graceful stop); (3) a chain of B=32 OOMs on the T~5070 cohort:
  teacher forward (fixed by `v4_capture_micro_batch`, `72706f7`, chunk 8→4→2),
  frozen-KV prefill (`17bc061`), teacher-forward-input residency (`7b2f015`
  host offload), and finally the training forward → micro_batch 16
  (`558c827`). **Probe SUCCESS (stage 3, 3 epochs): epoch 1 = 485 s
  (store-fill 177 s, gate-exempt), epochs 2–3 = 13.9 s at 82–86% util,
  capture_seconds = 0.** The fill-once-store lever confirmed at 26B: ~35×
  epoch-time drop after epoch 1. Full 4-stage relaunch queued behind the
  agpuh01 M1 legs.
- **02:07** M1 legs (task #6) launched on agpuh01 GPUs 1–2: four 0.6B
  PPP2 2-epoch runs at seed 17 — A resident-online reference, B scoped+
  fill-once store (expect ~1e-3 vs A + capture_s≈0 at epoch 2),
  C = B + full Adam (rotation reference), D = C + rotate (must be
  BIT-identical to C, Adam moments included). Configs `m1[a-d]_*.yaml`.
  Resident leg (m1a) running (launch pids 1256232/1256266); legs run sequentially.

- **02:25 owner framing (recorded in docs/training_pipeline_v4.md
  "Beyond the OOM wall"):** part of the contribution is that when
  traditional training OOMs, ours still trains — and when even the
  weights don't fit, it rotates. The M1 store+adam vs store+rotate (m1c vs m1d) bit-identity (Adam
  moments included) is the certification instrument for that claim; the
  31B/26B OOM diagnoses double as the traditional-baseline evidence.

## Overnight plan (set 02:35, owner asleep until ~06:00)

Two long MEMORIZATION tests (owner: "prove that our system also
memorises, not only that it runs") — the deliverable is recall-vs-epoch:

1. **gemma-31B PPP4, 50 epochs** (agpuh02, launch v4-20260718021132-
   3509303): micro_batch 8, gate floor 20, recall eval every 5 epochs,
   standard damage every 10, relay CE every epoch. ~9 min/epoch → ~8 h.
   The prior 40-epoch launch was gracefully stopped at ~epoch 2 and
   relaunched under this test identity.
2. **gemma-26B PPP4, 500 epochs** (agpuh01, config gemma4_26b_v4_ppp4_
   e500): the accumulated-dose run (~20× the einf dose), recall eval
   every 10 epochs (50 curve points), damage every 50. Chained to launch
   automatically when the M1 sequencer finishes
   (scripts/wait_m1_then_g26b_e500.sh → runs/g26b_e500_chain.log).

M1 status at handoff: resident leg (m1a) SUCCESS (both stages epoch 2); store leg (m1b) running
on GPUs 1–2 with the subprocess battery visibly exercising GPUs 0/3;
legs C/D + verdict (incl. Adam-moment bit-identity) follow. Verdict file:
runs/m1_verdict.txt.

Agent wake schedule: guardian ticks 03:17/04:17/05:17 (session must
outlive the 05:39 reauthorization), full morning review 05:50.
Deferred to morning: DeepSeek demo attempt 5 (triton backend), 26B/
DeepSeek locality certs, PPP8, 122B store lane.

## Morning review (08:10)

**Overnight failure, one root cause:** both memorization tests died at
~02:30–03:15 from the SAME regression — `_relay_segment` lost its
`@torch.no_grad()` during the night's refactors (present at `e6ceb0f`,
the einf era). The staged relay retained autograd graphs through every
LoRA block for all 65 cohorts → stage-0 memory crept to 79 GiB → OOM →
reaper killed the healthy siblings (26B stages 1–3 had reached epochs
26–35 at 14–20 s/epoch before being stopped — the speed story held).
Fixed `7991624`: decorator restored + cohort boundaries streamed to host
+ e500 relay cadence 10. Both tests relaunched 08:08
(g26b e500: v4-...080854-1275496; g31b x50: v4-...080854-3518168).

**M1 VERDICT (task #6, corrected comparisons over stage metrics):**
- resident-online vs scoped fill-once store: max_rel 2.0e-3,
  mean 6.0e-4 over 56 (layer,epoch) rows — inside the predicted bf16
  relay-boundary envelope. STORE LANE CERTIFIED at tolerance.
- **store+adam vs store+rotate (resident vs ROTATED weights+Adam moments): per-layer losses
  BIT-IDENTICAL, 56/56 rows.** Rotation is pure transport at the
  training level — the "beyond the OOM wall" certification instrument
  reads clean.
- Adam moments store+adam vs store+rotate: equal to ~1e-7 relative in exp_avg (exp_avg_sq
  mostly exact); attributed to nondeterministic backward atomics, NOT
  transport (losses bit-identical rules out systematic drift). TODO:
  C-repeat control to pin the noise floor.
- Known artifact: the sequencer's compare_v4_shard_numerics invocation
  expects single-process metrics; staged runs need the per-stage
  comparison used above.

**New standing rule (owner):** every launch gets a 15-minute
verification cron with relaunch authority (memory:
launch-verification-every-15min).

## Inter-node fabric measurements (09:55, for PPP8 / BSC planning)

Both nodes: 2× ConnectX-6 HDR-200 (mlx5_0=ib0, mlx5_1=ib1), same IB
subnet (LIDs 116/117, one SM), plus 10 GbE management (172.21/16).

| path | measured | notes |
|---|---|---|
| RDMA mlx5_0 (ib_write_bw, 64KB msgs) | **195.9 Gb/s** (~24.5 GB/s) | line rate |
| RDMA mlx5_1 | **192.2 Gb/s** | second rail also line rate; ~49 GB/s aggregate |
| Lustre write (2 GB, O_DIRECT, 1 stream) | 398 MB/s | relay envelope write path |
| Lustre cross-node read | 1.2 GB/s | relay envelope read path |
| Ethernet | 10 GbE (1.25 GB/s ceiling) | control/ssh only |

Anomalies: **IPoIB node-to-node is broken** (ARP incomplete both rails,
100% ping loss) while node→Lustre-server IPoIB works — looks like
IPoIB neighbor-discovery/partition config, NOT fabric (RDMA between the
same nodes runs at line rate). Socket-based relay transport would need a
static neighbor entry (root) or the 10 GbE. Also both IB interfaces
share one /16 with conflicting kernel routes (metrics 150/151) — any
future socket code must bind interfaces explicitly.

PPP8 implication: the Lustre-file relay (current design) moves a ~30 GB
27B boundary in ~100 s single-stream — fine at relay-every-10 cadence;
per-cohort concurrent writes would parallelize it. Raw RDMA holds a
~25-50× reserve if cross-node relays ever become the bottleneck (BSC
scale, store-fill relay lane). ib_write_bw servers need
setsid+</dev/null over ssh (die silently otherwise); control channel
must use the Ethernet IPs.

- **10:20** GPU-history samplers started on BOTH nodes (2 s cadence,
  `runs/gpu_history_agpuh0{1,2}_20260718.csv`: timestamp, index, mem,
  util, mem-util, power) for post-analysis of the observed oscillation.
  e500 layer map: GPU0=blocks 1–8+embed+relay-head+battery, GPU1=9–16,
  GPU2=17–23, GPU3=24–30+eval tail. First samples show the pattern:
  stages 1/2 idle at the epoch barrier while 0/3 finish (independent
  stage speeds + relay/battery sync points every 10 epochs).

## Epoch-300 results check + the stop incident (10:50–11:10)

**26B e500 memorization verdict at 300 epochs: FLAT.** Recall
(overall_word_acc) machado 0.123→0.099, quijote_ch1 0.190→0.180 (wiggle
to 0.21 at 200), quijote_ch4 0.133→0.135; next-token acc flat; mean
layer loss −1.5%; standard macro accuracy 0.375→0.354 (mild damage
onset). At lr 1e-6 with r16 adapters, dose (epochs) does NOT express —
and barely stores — on 26B. This is the run's scientific product: the
lever is update magnitude (#9), not epoch count.

**Stop incident (bug found, task #17):** graceful TERM at epoch 300 was
honored ONLY by stage 0 (stopped 311, checkpoint saved). Stages 1–3
received TERM twice (per-PID delivery verified) and trained through to
epoch 500 — cooperative_stop_signals is ignored on non-zero stages.
Consequences: blocks 1–8 have 311 epochs, blocks 9–30 have 500 (uneven
dose — flat recall makes this benign); recall evals ceased at 310 with
stage 0. The planned 31B stop-at-25 was CANCELLED (stopper killed at
epoch 20) to avoid beheading its recall evals the same way; the 31B runs
its full 50 clean. Final 26B checkpoint will be merged + offline-evaled
when the stages finish their relay-eval backlog.

## Market-price comparison (owner request; tinker-docs.thinkingmachines.ai/tinker/models, fetched 2026-07-18)

Tinker (Thinking Machines) LoRA fine-tuning, prices per MILLION training
tokens: Qwen3.6-35B-A3B $1.177 · Qwen3.5-397B-A17B $6.60 ·
Qwen3.5-9B $1.463 · Qwen3.5-4B $0.737 · GPT-OSS-120B $0.737 ·
Kimi-K2.6 $4.84 · Nemotron-3-Ultra-550B $5.478. MoE priced by ACTIVE
params; checkpoint storage $0.10/GB/month.

Billing categories (their terms): Prefill = forward-only on input
tokens; Cached prefill = 80% off on prompt-cache hits; Sample =
generation; Train = forward+backward on gradient tokens. Peer prices
(35B-A3B / 397B-A17B): prefill $0.54 / $3.00, cached $0.108 / $0.60,
sample $1.335 / $7.50, train $1.177 / $6.60 per M tokens.

Our measured token split (cache index, exact): 718,877 teacher positions
per epoch over 2,071 items = 628,490 prompt (prefillable, cacheable
after epoch 1) + 90,387 answer (trained). The v5rs_window dataset has
short windowed contexts (~304 prompt tokens/item), so prefill terms are
modest here; full-RAG corpora (~2.5k prompts) shift the balance hard
toward prefill.

- **26B run, 300 epochs (peer 35B-A3B):** Tinker equivalent =
  epoch 1 (0.6285M×$0.54 + 0.0904M×$1.177 = $0.45) + 299 cached epochs
  × (0.6285M×$0.108 + 0.0904M×$1.177 = $0.174) ≈ **$52.6** (+$0.12
  one-off answer generation at sample price). Our cost: ~10 H100-hours
  ≈ **$25–30** rental — ≈2× cheaper on this short-prompt dataset. Their
  bill is 61% train / 39% cached prefill here; on long-prompt corpora
  the recurring prefill term dominates and fill-once (we pay the
  prompt once, ever) widens the gap substantially.
- **31B run (50 epochs, dense):** NO dense-30B peer listed (their
  MoE-active-param pricing leaves large dense out; nearest anchor 9B
  dense $1.463/M train). Our cost ≈ 18 H100-hours ≈ $45.
- **397B lane, 30-epoch projection:** epoch 1 $2.48 + 29×$0.974 ≈
  **$30.7 at Tinker** — the number our rotate-lane run must beat when
  #7/M5 land (our side: single reserved node × wall hours).

Caveat kept: price-per-OUTCOME is not yet comparable — our lr 1e-6 dose
stored but did not express (the lr arm decides that side).

## e500 finale post-mortem (12:00, verdict from the Fable investigator)

The three non-zero stages died at ~11:27 on the relay wait's 3600 s
timeout, BEFORE certification/checkpoint — the 500-epoch adapters of
blocks 9–30 are LOST (stage 0's 311-epoch checkpoint for blocks 1–8
survives; all training metrics survive). Cause chain: (1) stop bug #17
let stages 1–3 train to 500 while stage 0 stopped at 311; (2) stopped
stages skip relay.drain, so the siblings blocked forever on stage-0
boundaries that would never come; (3) MY first rescue wrote the
fabricated envelopes to the Lustre fallback root — the fabricator ran
without SELFUPDATE_V4_RELAY_ROOT, so the stages never saw them
(operator error; the second, correct flood landed after the fuses).
Scientific cost: minimal — the flat-recall verdict was measured live
through epoch 310 and layer losses through 500; only the 500-epoch
endpoint eval is unobtainable. Operational lessons:
- FIXED: relay drain is now best-effort — on timeout it logs
  relay_drain_abandoned and PROCEEDS to certification + checkpoint
  (evals are telemetry; checkpoints are the run).
- OPEN #17: the cooperative-stop bug is the root cause and gets a
  0.6B repro + fix before any further multi-stage stop is attempted.
- Rescue procedure law: any relay fabrication MUST export the same
  SELFUPDATE_V4_RELAY_ROOT and launch id as the target processes, and
  verify consumption (file disappearance) within one poll interval.

## PPP5 bring-up chronicle (afternoon) + first cross-node epoch

Five launch attempts, each killing one real defect (all committed):
(1) device-uniqueness law was global, not host-scoped; (2) launcher
didn't forward STAGE_HOSTS to remote stages; (3) remote-pid command
substitution froze on HEALTHY stages (sshd holds the session; even
stdin=/dev/null doesn't release it) → pidfile-over-Lustre spawn pattern;
(4) attempt-4 chimera: my pkill/ps pattern said `ppp5x` but cmdlines
carry `ppp5_xnode` — attempt-3 zombies survived unseen, published stale
adapter mail into the shared exchange, and the envelope check (working
as designed) killed attempt-4's stage 0 at its first battery. The
occupancy-map-first rule is what exposed it. Lesson recorded: process
patterns must match CMDLINE strings, not run names; the GPU map is the
ground truth.

**Milestone regardless: the FIRST cross-node v4 training epoch
completed** (attempt 4, before the stale-mail kill): stage 0 on agpuh01
finished epoch 1 and boundary envelopes crossed hosts via the Lustre
relay (e0001/e0002 consumed on agpuh02). B7's core mechanism is
demonstrated; attempt 5 (clean field, fixed launcher) runs the full
25-epoch experiment.

Also this hour: duplicate 122B rotor found sharing GPU1 (same pattern
class) and killed; rotor trio relaunched 13:05 with the battery-exchange
fixes (solo relay root + inherited launch id + cadence-gated epoch-0
battery); 31B rotor reached its on-card epoch-0 battery (57 GB child on
GPU0, CVD-confined ✓); dequant v2 past 105/316 GiB.

## Queue (priority order)

1. DeepSeek demo attempt 4 on agpuh02 (kv-cache fp8 fix) — front gate.
2. gemma-31B PPP4 relaunch from HEAD (micro_batch 16) — after demo.
3. gemma-26B: await Opus probe verdict, integrate fix myself.
4. #11 `_FrozenDeepseekCtx` (code, in progress, no GPU needed).
5. #6 M1 store/rotate numerics proof — first free 4-card window.
6. #7 122B store-lane epoch timing; #10 PPP8 cross-node; #9 lr sweep.

## Model handoff: Fable 5 -> Opus 4.8 (owner, 2026-07-18 ~14:30)

From this point the operator model is **Opus 4.8 (1M context)**. Handoff
state below; all prior entries in this notebook were authored under
Fable 5.

- **Rotation certified as near-free**: the M1 store+adam vs store+rotate
  cert proved the rotated numbers bit-identical incl. Adam moments, and the
  measured `rotation_stall` is 0.04–0.29 s across every completed rotary run
  (27B/31B/35B/122B — transport ~99.9% hidden by the pinned ping-pong
  prefetch). CORRECTION (2026-07-18 audit): the "26B rotary PPP1 = 61.8 s"
  originally cited here was contaminated — it was a 27B, 100-item, PP4
  throughput (61.84 tok-ev/s) mis-transcribed as a 26B 1-GPU s/epoch. The
  26B rotary run died after store-fill with no completed epoch; re-queued.
  The near-free CONCLUSION stands on the surviving evidence above.
- **Live at handoff**: PPP5 cross-node (stage0 epoch 1 done, Lustre
  relay carrying boundaries), 31B rotor (epoch-0 battery on GPU0),
  122B rotor (GPU1, layer-outer store-fill #18 now active), dequant v2
  (~200/316 GiB). GPU2 free (26B rotor finished + crashed on the cert
  KeyError now fixed in f445403).
- **Open code, no GPU needed**: #20 (native v4 report schema), #19
  (weight-transport matrix). GPU-gated: #17 (stop-bug repro), #16->#11
  (dequant done -> DeepSeek cert), #7/#21 (envelope rows), #14
  (DeepSeek vLLM demo).
- **The speed table** (docs/training_pipeline_v4.md) is the "speed
  proven" deliverable: 5/9 rows measured; the four open rows each name
  their unblocking task. Speed is NOT proven until all nine are filled.
- Operator doctrine accumulated today (all in memory + docs): 15-min
  launch verification, kill-doomed-runs, occupancy-map-first,
  launch-expectation recipe, pattern-vs-cmdline, /home-never-cache.

## DeepSeek-V4-Flash vLLM answer-gen: DEFINITIVELY BLOCKED (16:05)

DeepSeek's fp4/mxfp4 experts cannot run in vLLM 0.25 on driver 565 /
H100 through ANY MoE backend. All four the mxfp4-conversion path accepts
were tried and each failed at load, before generation:
- MARLIN: "PTX compiled with an unsupported toolchain" (driver 565 can't
  JIT the prebuilt Marlin fp4 kernels)
- TRITON: "kernel does not support MoEActivation.SILU"
- EMULATION: "Unsupported mxfp4_backend for Mxfp4MoEMethod" (conversion
  rejects EMULATION; only TRTLLM/Triton/AITER/XPU)
- FLASHINFER_TRTLLM: "kernel does not support current device cuda"
The bf16 dequant snapshot avoids fp4 but is 526 GB = 131 GB/card at TP4,
OOMs (doesn't fit 4 cards). Verdict: DeepSeek answer-gen needs EITHER a
newer driver/vLLM with working H100 mxfp4 kernels, OR a transformers
model.generate fallback on the bf16 snapshot with CPU offload (slow).
DEFERRED — not worth blocking the speed campaign. The B8 TRAINING adapter
(deepseek_ctx.py) is unaffected and ready; only the answer front-gate is
blocked. Note: 397B experts are fp8 (not fp4/mxfp4), so 397B will NOT hit
this — its vLLM path uses the fp8 kernels.

## 35B-A3B front gate: healthy (16:05)
Standard bf16 MoE (no fp4). Loading TP4 on agpuh01 (19.9 GB/card, all 4
workers up, FlashInfer GDN prefill JIT-compiling). Watcher building the
cache on success. This is the clean smaller-model row.
