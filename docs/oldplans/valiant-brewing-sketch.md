# Campaign 2: Saturation, Quijote, Thinking-Selective, and the Road to the Evolving Person

*(Supersedes the Campaign-1 plan; Campaign 1 closed 2026-07-04 ~07:25 —
results in EXPERIMENTS.md "CAMPAIGN CLOSING TABLE" and paper/paper1.md.
The thinking_selective design from Campaign 1 is carried forward in
Part 3 below.)*

## Context

Campaign 1 solved Machado at 0.6B: final recipe `vocab_mse` + maieutic
v4 + tail-CE k=8 + anchor-KL recites all 715 verses (first error v708),
answers any elicitation style at 100%, at +0.51 nats mean forgetting.
The user now directs Campaign 2 with budget until **Sunday ~16:00
(~32 h)**, with standing instruction to invent new objectives if ahead
of budget. Goals, from the user plus Campaign-1 future work:

1. **Reasoning-family resistance** (Phi/gpt-oss fail; think channels
   bypass the readout) — attack with the designed-but-unbuilt
   `thinking_selective` mask.
2. **Capacity scaling law**: readout width k as a *budget* across
   content size — the Quijote question in miniature.
3. **Tuned-lens program** (pending from Campaign 1's Wave I plan).
4. **Final recipe at 4B/8B/14B**, then **Don Quijote data engineering**.
5. **Saturation**: how much content fits before the method becomes
   destructive, per model — "go to the Quijote if Machado is already
   perfect" — with **sophisticated destruction tests** (metrology v2).
6. **Evolving-person groundwork**: the future phase moves conversations
   into weights by censoring the student on OLDER context (possibly
   older-but-high-attention-scored). This campaign delivers full
   comprehension of the memorization phase + a design doc; a first
   prototype is a stretch objective.

Scope defaults taken in the user's absence (revisable): ~32 h budget;
saturation+Quijote thread leads; destruction metrology = probe battery
+ benchmark CE-ranking + intrusion-generation rate + degeneration
counters; evolving person = design doc committed, prototype stretch.

## (skeleton — sections being filled after reconnaissance)

- Part 1: Destruction metrology v2 (build first — every later arm needs it)
- Part 2: Saturation ladder + Quijote pipeline + capacity law
- Part 3: thinking_selective (carried design) + reasoning-family arms
- Part 4: Scale ladder 4B/8B/(14B)
- Part 5: Tuned-lens program
- Part 6: Evolving-person comprehension + design doc (+ stretch prototype)
- Part 7: Stretch objectives pool (new losses on approved methods, etc.)
- Schedule, autonomy protocol (30-min reports), verification, risks
